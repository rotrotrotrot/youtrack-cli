# INSTALL

```
$ sudo pip3 install -r requirements.txt
$ cp you.py ~/.local/bin/you
$ cp youtrack-sample.yml ~/.youtrack.yml

# For zsh integration
$ echo "source ${PWD}/zsh.plugin.zsh" >> ~/.zshrc
```
